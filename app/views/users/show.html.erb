<div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image(100,100) %>
  <% if @user == current_user %>
   <p><%= link_to "User info", edit_user_path(@user) %></p>
  <% end %>
</div>
<!-- 上記はapplication.htmlerbのサイドバーに記述 -->

<%= render 'books/list', books: @books %>